const navbarStyles = {
  icons: {
    default:
      'w-4 h-4 hover:hue-rotate-180 hover:invert hover:brightness-[0.1] cursor-pointer sm:w-auto sm:h-auto',
    active: 'hue-rotate-180 invert brightness-[0.1] cursor-pointer',
  },
};

export default navbarStyles;
